import { EGPcurrencyPipe } from './egpcurrency.pipe';

describe('EGPcurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new EGPcurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
